REM FishBot uses `cloc` to count lines of code which are used to populate the 'Fun Stats' section in `FishBot.js`. 
REM To use it, download the latest `cloc` .exe from the cloc repo: (https://github.com/AlDanial/cloc/releases#release-v2.08) and place it into this folder.
REM In this example, `cloc-2.08.exe` is used.

cloc-2.08.exe --include-lang=JavaScript ..\multiplay\skirmish\

pause